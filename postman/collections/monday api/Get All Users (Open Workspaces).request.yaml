$kind: http-request
url: "{{monday_base_url}}"
method: POST
headers:
  Authorization: "{{MONDAY_API_TOKEN}}"
  API-Version: 2024-07
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "query": "query { users(limit: {{page_size}}, page: {{page}}, kind: all) { id name email is_admin is_guest enabled created_at last_activity is_view_only teams { name }  } }"
    }
order: 2096
